<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Crear Producto</title>
</head>
<body>
    <h1>Crear Nuevo Producto</h1>

    <% if (typeof mensaje !== 'undefined') { %>
        <div class="alert alert-<%= tipo === 'success' ? 'success' : 'danger' %>">
            <p><%= mensaje %> </p>
        </div>
    <% } %>

    <form action="/productos/crearProducto" method="POST">
        <label for="descripcionProducto">Descripción del Producto:</label>
        <input type="text" id="descripcionProducto" name="descripcionProducto" required>
        <br>
        <label for="precioCompra">Precio:</label>
        <input type="number" id="precioCompra" name="precioCompra" required>
        <br>
        <label for="numeroVendedor">Número de Vendedor:</label>
        <input type="number" id="numeroVendedor" name="numeroVendedor" required>
        <br>
        <label for="idComprador">ID de Comprador:</label>
        <input type="number" id="idComprador" name="idComprador" required>
        <br>
        <button type="submit">Crear Producto</button>
    </form>
    <a href="/productos/obtenerProductos">Volver</a>
</body>
<style>

    h1 {
        text-align: center;
    }
    body {
        margin: 0 auto;
        width: 500px;
        font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
        background-color: #f2f2f2;
    }

    form {
        display: flex;
        flex-direction: column;
    }

    p {
        color: green;
        font-weight: 600;
    }

    input {
        margin-top: 5px;
        padding: 5px;
        border: 2px solid grey;
        border-radius: 5px;
    }
    button {
        background-color: grey;
        border: 2px solid grey;
        color: #fff;
        font-weight: 600;
        padding: 5px;
        border-radius: 5px;
        cursor: pointer;
    }
</style>
</html>
