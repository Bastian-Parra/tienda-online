<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Crear Vendedor</title>
</head>
<body>
        <h1>Crear Vendedor</h1>

        <% if (typeof mensaje !== 'undefined') { %>
            <div class="alert alert-<%= tipo === 'success' ? 'success' : 'danger' %>">
                <p><%= mensaje %> </p>
            </div>
        <% } %>

        <form action="/vendedor/crearVendedor" method="POST">
                <label for="nombreVendedor">Nombre del Vendedor</label>
                <input type="text" id="nombreVendedor" name="nombreVendedor" class="form-control" required>
            <button type="submit" class="btn btn-primary">Crear Vendedor</button>
        </form>
    <a href="/vendedor/obtenerVendedores">Volver</a>
</body>
<style>

    h1 {
        text-align: center;
    }
    body {
        margin: 0 auto;
        width: 500px;
        font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
        background-color: #f2f2f2;
    }

    form {
        display: flex;
        flex-direction: column;
    }

    p {
        color: green;
        font-weight: 600;
    }

    input {
        margin-top: 5px;
        padding: 5px;
        border: 2px solid grey;
        border-radius: 5px;
    }
    button {
        background-color: grey;
        border: 2px solid grey;
        color: #fff;
        font-weight: 600;
        padding: 5px;
        border-radius: 5px;
        margin-top: 20px;
        cursor: pointer;
    }
</style>
</html>