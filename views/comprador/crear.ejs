<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Crear Comprador</title>
</head>

<body>
    <h1>Crear Nuevo Comprador</h1>

    <% if (typeof mensaje !=='undefined' ) { %>
        <div class="alert alert-<%= tipo === 'success' ? 'success' : 'danger' %>">
            <p>
                <%= mensaje %>
            </p>
        </div>
        <% } %>

            <form action="/comprador/crearComprador" method="POST">
                <label for="nombreComprador">Nombre del Comprador</label>
                <input type="text" id="nombreComprador" name="nombreComprador" required>
                <br>
                <button type="submit">Crear Comprador</button>
            </form>
            <a href="/comprador/obtenerCompradores">Volver</a>
</body>

<style>

    h1 {
        text-align: center;
    }
    body {
        margin: 0 auto;
        width: 500px;
        font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
        background-color: #f2f2f2;
    }

    form {
        display: flex;
        flex-direction: column;
    }

    p {
        color: green;
        font-weight: 600;
    }

    input {
        margin-top: 5px;
        padding: 5px;
        border: 2px solid grey;
        border-radius: 5px;
    }
    button {
        background-color: grey;
        border: 2px solid grey;
        color: #fff;
        font-weight: 600;
        padding: 5px;
        border-radius: 5px;
        cursor: pointer;
    }
</style>
</html>